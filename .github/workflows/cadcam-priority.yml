name: CAD CAM core fast validation

on:
  pull_request:
    branches:
      - Test
  workflow_dispatch:

permissions:
  contents: read
  actions: read

concurrency:
  group: cadcam-core-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  cadcam-core:
    runs-on: windows-2022
    steps:
      - name: Enable Git long paths
        shell: pwsh
        run: |
          git config --global core.longpaths true
          git config --global core.protectNTFS false

      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Restore partial DLL bootstrap cache
        continue-on-error: true
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          New-Item -ItemType Directory -Force .repair/bootstrap-bin | Out-Null
          gh run download 31432391159 --repo $env:GITHUB_REPOSITORY --name cadcam-core-dll --dir .repair/bootstrap-bin
          if ($LASTEXITCODE -ne 0) {
            Write-Warning 'Bootstrap artifact could not be downloaded; continuing with source-only dependency resolution.'
            exit 0
          }
          $dlls = @(Get-ChildItem .repair/bootstrap-bin -Recurse -File -Filter *.dll)
          "Bootstrap DLL count: $($dlls.Count)" | Tee-Object build-logs/bootstrap-dlls.txt
          $dlls | Sort-Object Name | ForEach-Object { $_.FullName } | Add-Content build-logs/bootstrap-dlls.txt

      - name: Repair recovered sources
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force build-logs | Out-Null
          ./.repair/cadcam-repair.ps1
          ./.repair/project-normalize.ps1
          ./.repair/source-compat-repair.ps1

      - name: Audit core dependency manifest
        continue-on-error: true
        shell: pwsh
        run: ./.repair/core-dependency-manifest.ps1

      - name: Build buClass blocker
        shell: pwsh
        run: |
          $log = 'build-logs/buClass-build.txt'
          msbuild 'Decompiled/buClass/buClass.csproj' /restore /m /t:Rebuild /p:Configuration=Release /v:minimal 2>&1 | Tee-Object $log
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Collect buClass DLL
        if: always()
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force core-dll | Out-Null
          Get-ChildItem -Recurse -File -Filter buClass.dll | Where-Object { $_.FullName -match '\\bin\\Release\\' } | ForEach-Object { Copy-Item $_.FullName core-dll -Force }

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: buclass-build-logs
          path: build-logs
          if-no-files-found: warn

      - name: Upload buClass DLL
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: buclass-dll
          path: core-dll
          if-no-files-found: warn
